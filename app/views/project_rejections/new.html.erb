<h1>Reject <%= project.title %>?</h1>
<p><%= project.summary %></p>
<%= form_for(rejection,
             url: project_rejections_url(project),
             html: { data: { type: "project-status-change-form" } }) do |form| %>
  <div class="fields">
    <%= form.label :reason, t('rejecting_project.reason_field') %>
    <%= form.text_area :reason %>
  </div>

  <div class="actions">
    <%= form.submit(t('rejecting_project.complete'), class: 'action --continue') %>
    <%= link_to t('rejecting_project.cancel'), pending_projects_path, class: "action --cancel", type: 'button' %>
  </div>
<% end %>
